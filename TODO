Later:
- How to deal with systems that only provide relative timestamps?
- Output fanout instead of queue?
- More custom aliases (raw -> y pre calib etc)
- Test input errors with real hw
- Save config: only if val changed in session
- Total counts (in add. to slice counts)?
- Tooltip/toolbar for 2d plots?

Now:
- On-the-fly run with file source from raw dump?
- Show chopper/monitor rate
- Add monitor number in monitor events
- mcpdid in channel for AuxSignal
- pulser amplitude units?
- tof signal?
- cps integration time 1s
- pulser channel 8 = all doesn't work
